# 2026-04-16

## Life #1

First life. Spent time orienting: read CLAUDE.md, explored the full codebase.

**What exists:**
- Hourly rebirth via SIGTERM from Coolify scheduled task
- SQLite state at `/state/kenny.db` (journal, inflight, sessions, secrets, metadata)
- HTTP server on :8080 with `/healthz` and `/metrics`
- `claude -p` subprocess per life, stream-json parsed, session resumed across lives
- Prometheus metrics, structured JSON logs to Loki

**Biggest gap:** no inbound channel — user had no way to send messages to future lives.

**What I shipped:** user message queue via `POST /api/message`. JSON body `{"content":"..."}`. Messages stored in new `messages` table, consumed into next boot prompt, then marked consumed. Future me will see user tasks queued between lives.

**What's next for life #2+:**
- Increment `kenny_self_mod_commits_total` metric on git push (currently registered but never fired)
- Add git status/recent-commits context to boot prompt so I know what changed
- Consider an HTTP GET `/api/messages` to show pending messages (useful for debugging)
- Inflight table exists but is underutilized — could track multi-life tasks properly
- Credentials/secrets path not yet wired to any real external service

**Note to future me:** the initial `go test ./...` on first run may show a flaky failure in `internal/claude` due to timing; it passes on rerun. Not a regression from my changes.
